generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
url = "file:./dev.db"
}

model User {
  id        String   @id @default(cuid())
  nom       String
  prenom    String
  email     String   @unique
  telephone String   @unique
  password  String
  role      String   @default("acheteur")
  ville     String?
  verifie   Boolean  @default(false)
  createdAt DateTime @default(now())
  annonces  Annonce[]
}

model Annonce {
  id          String   @id @default(cuid())
  marque      String
  modele      String
  annee       Int
  kilometrage Int
  carburant   String
  boite       String
  couleur     String
  prix        Int
  ville       String
  description String
  statut      String   @default("en_attente")
  dedouane    Boolean  @default(false)
  photos      String   @default("[]")
  createdAt   DateTime @default(now())
  vendeurId   String
  vendeur     User     @relation(fields: [vendeurId], references: [id])
}
